cat > package.json << 'EOF'
{
  "name": "vulnerable-packages-test",
  "version": "1.0.0",
  "description": "Intentional vulnerable dependencies for scanner validation",
  "private": true,
  "dependencies": {
    "lodash": "4.17.20",
    "minimist": "0.0.8",
    "axios": "0.21.1",
    "express": "4.17.1",
    "jquery": "3.4.1"
  },
  "devDependencies": {
    "serialize-javascript": "2.1.1"
  },
  "scripts": {
    "start": "node index.js",
    "sbom": "npm sbom --sbom-format=cyclonedx --json > sbom.cdx.json"
  }
}
EOF
